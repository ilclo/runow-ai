package ai.runow.ui.renderer

import androidx.compose.ui.Modifier

/** Aggiunge un leggero effetto “pulse” (scala/alpha) quando selected = true. */
fun Modifier.pulseOnSelect(selected: Boolean): Modifier
